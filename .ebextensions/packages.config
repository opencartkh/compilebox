packages:

  yum:
    gcc-c++: []
    make: []


container_commands:
  01_docker:
    command: "yum-config-manager --enable epel; yum install -y docker-io; service docker restart ; pwd; docker build -t 'virtual_machine' - < /var/app/ondeck/Setup/Dockerfile"
