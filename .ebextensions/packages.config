packages:

  yum:
    gcc-c++: []
    make: []


container_commands:
  01_docker:
    command: "yum-config-manager --enable epel; yum install -y docker-io; /usr/sbin/service docker restart ; pwd; ls; docker build -t 'virtual_machine' - < Setup/Dockerfile"
