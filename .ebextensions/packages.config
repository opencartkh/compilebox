packages:

  yum:
    gcc-c++: []
    make: []


commands:
  01_docker:
    command: "yum-config-manager --enable epel; yum install -y docker-io; service docker restart ; docker build -t 'virtual_machine' - < Dockerfile"
